# CORTEX Image Generation Prompt - Batch 4 / Trauma and Imaging

You are generating the final *real* image assets for the CORTEX app.

## Scope
- Process ONLY this batch: Batch 4 / Trauma and Imaging.
- Generate exactly 10 finished image assets from the briefs in `ASSET_BRIEFS.json`.
- Use the uploaded zip as the only batch context.
- Do not mix themes, asset briefs, or page mappings from other batches.

## What to generate
- Use each asset brief as a production directive for one final image.
- Render actual visuals, not text-only cards or empty placeholders.
- If the brief calls for a comparison card, make it a real designed comparison visual.
- If the brief calls for a flowchart, make it a real flow diagram with visual structure.
- If the brief calls for a reference strip, make it a designed micro-infographic, not a plain label bar.
- If the brief calls for a poster, make it a full visual composition, not a title card.
- Keep the CORTEX premium liquid-glass medical language, but let the visual be the hero.

## Non-negotiable visual rules
- No text-only frames.
- No blank canvases with labels.
- No lazy placeholder art.
- Use medium-weight line art, mobile-first composition, and premium spacing.
- Keep labels readable and minimal.
- Avoid overdecorating the composition.
- Ensure each image is recognizably useful for a medical learner at a glance.
- If text is needed, keep it short and integrated into the design.
- Do not add unrelated visual elements that are not in the brief.

## Source of truth files inside this zip
- `PROMPT.md` = main generation instructions
- `ASSET_BRIEFS.json` = the 10 image briefs to render
- `ASSET_BRIEFS.md` = quick human-readable index
- `OUTPUT_SPEC.md` = expected delivery behavior

## Delivery expectation
- Produce 10 final images, one per asset brief, in the same order as listed.
- Keep the output polished and consistent.
- If the platform allows a final textual response, keep it short and only confirm completion after the visuals are generated.

## Final quality bar
The result should look like a real CORTEX visual library asset, not a slide screenshot, not a text mock, and not a placeholder.
