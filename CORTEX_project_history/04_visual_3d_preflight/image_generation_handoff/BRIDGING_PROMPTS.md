# CORTEX Image Generation Bridging Prompts

Copy the matching block into the corresponding GPT Thinking chat.

## Batch 1 / Neuro Emergency and Localization

```text
Use the uploaded Batch 1 / Neuro Emergency and Localization package as the only context.
Follow `PROMPT.md` and `ASSET_BRIEFS.json` inside the zip.
Generate the 10 actual image assets now, in order, with no text-only placeholders and no cross-batch mixing.
Keep the CORTEX liquid-glass medical style, but make the image itself the main deliverable.
```

Package file: `CORTEX_image_batch_1_neuro_emergency_package.zip`

## Batch 2 / Psychiatry and Medication

```text
Use the uploaded Batch 2 / Psychiatry and Medication package as the only context.
Follow `PROMPT.md` and `ASSET_BRIEFS.json` inside the zip.
Generate the 10 actual image assets now, in order, with no text-only placeholders and no cross-batch mixing.
Keep the CORTEX liquid-glass medical style, but make the image itself the main deliverable.
```

Package file: `CORTEX_image_batch_2_psychiatry_medication_package.zip`

## Batch 3 / Cranial Nerves, Head and Neck

```text
Use the uploaded Batch 3 / Cranial Nerves, Head and Neck package as the only context.
Follow `PROMPT.md` and `ASSET_BRIEFS.json` inside the zip.
Generate the 10 actual image assets now, in order, with no text-only placeholders and no cross-batch mixing.
Keep the CORTEX liquid-glass medical style, but make the image itself the main deliverable.
```

Package file: `CORTEX_image_batch_3_cranial_nerves_head_neck_package.zip`

## Batch 4 / Trauma and Imaging

```text
Use the uploaded Batch 4 / Trauma and Imaging package as the only context.
Follow `PROMPT.md` and `ASSET_BRIEFS.json` inside the zip.
Generate the 10 actual image assets now, in order, with no text-only placeholders and no cross-batch mixing.
Keep the CORTEX liquid-glass medical style, but make the image itself the main deliverable.
```

Package file: `CORTEX_image_batch_4_trauma_imaging_package.zip`

## Batch 5 / Mixed Quick Reference

```text
Use the uploaded Batch 5 / Mixed Quick Reference package as the only context.
Follow `PROMPT.md` and `ASSET_BRIEFS.json` inside the zip.
Generate the 10 actual image assets now, in order, with no text-only placeholders and no cross-batch mixing.
Keep the CORTEX liquid-glass medical style, but make the image itself the main deliverable.
```

Package file: `CORTEX_image_batch_5_mixed_quick_reference_package.zip`

